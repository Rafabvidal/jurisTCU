S3_BUCKET_NAME='projeto-trt6'

# SMTP — usado pelo search-notifier para enviar alertas de buscas salvas.
# Se SMTP_USER/SMTP_PASSWORD ficarem vazios, o serviço apenas loga os e-mails.
SMTP_HOST='smtp.gmail.com'
SMTP_PORT='587'
SMTP_USER=''
SMTP_PASSWORD=''
SMTP_SENDER=''
